<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/main_container_charaf"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="16dp">

    <TextView
        android:id="@+id/charaf_label_status"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Statut : prêt"
        android:textSize="16sp"
        android:paddingBottom="12dp"/>

    <ProgressBar
        android:id="@+id/charaf_progress_indicator"
        style="?android:attr/progressBarStyleHorizontal"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:max="100"
        android:progress="0"
        android:visibility="invisible"/>

    <ImageView
        android:id="@+id/charaf_display_image"
        android:layout_width="120dp"
        android:layout_height="120dp"
        android:layout_marginTop="16dp"
        android:src="@mipmap/ic_launcher"
        android:contentDescription="image"/>

    <Button
        android:id="@+id/charaf_btn_load_thread"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Charger image (Thread)"
        android:layout_marginTop="16dp"/>

    <Button
        android:id="@+id/charaf_btn_compute_async"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Calcul lourd (AsyncTask)"
        android:layout_marginTop="8dp"/>

    <Button
        android:id="@+id/charaf_btn_show_toast"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Afficher Toast"
        android:layout_marginTop="8dp"/>

</LinearLayout>